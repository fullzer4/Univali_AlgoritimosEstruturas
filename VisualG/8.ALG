algoritmo "helloworld"
var
   horas, dias, salario: real
inicio
   escreva("insira a quantidade de dias trabalhados: ")
   leia(dias)
   horas <- dias * 8
   salario <- horas * 50
   escreva("seu salario e: ", salario)

fimalgoritmo